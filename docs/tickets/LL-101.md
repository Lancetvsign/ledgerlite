# LL-101 — Cash in transit done right: three report states, tightened single-sided rule, aged gate, auto-link

**Status** proposed (Gate 7) · **Origin** `docs/GATE-7.md` §6

Gate 7 H2, M2, L7, L8, 5c L1. Report rows: mirrored / in transit (amber, amount, age of the oldest open group) / MISMATCH — never 'Mirrored' while in transit ≠ 0; 'single-sided' = an INTERCOMPANY entry whose `source_id` is a POSTED `bank_import_lines` row of the same company whose `chosen_account_id` is the pair account; the partner `not exists` bounded by `posting_date <= asOf`; `assertIntercompanyMirror` fails on single-sided groups older than the window + 30 days; candidates excluded when the viewer already marked its own side, and a mark auto-joins a mirror single-sided group of the same |amount| in the window; per-submit candidate allocation. Tests: unequal independent marks (5000 vs 4500) → MISMATCH; a group-bearing raw one-sided insert → detected; as-of between the two statement dates → mirrored.
