# LL-104 — Structural line immutability: BEFORE INSERT guard on journal_lines (owner's decision)

**Status** proposed (Gate 7) · **Origin** `docs/GATE-7.md` §6

Gate 7 M5 / 5c L4. Plan mode. `journal_lines_immutable` also on BEFORE INSERT, with a session-local escape (`set local ledgerlite.posting = on`, read via `current_setting(..., true)`) set only inside `postEntryCore` / `reverseEntryCore`; `CHECK (source_type <> 'REVERSAL' or reversal_of_id is not null)`; the 0010 period guard reconsidered for DRAFT→POSTED updates (5c N4). Corruption tests: raw line append to a POSTED entry refused; the posting paths unaffected.
