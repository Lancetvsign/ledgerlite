# LL-103 — Gate 7 housekeeping: authorization fence, audit completeness, isolation attempts, idempotent join/leave

**Status** proposed (Gate 7) · **Origin** `docs/GATE-7.md` §6

Gate 7 M6, L1–L5, L10, L11. `requireCompanyMembership` in `transferCounterparts`; lint fence covers `*/shared` and `*/intercompany`; structured `closedIn` instead of `?detail=`; report filters former members (or labels them); joins audited in every ACTIVE member and pair deactivation names the leaver; isolation attempts for the report, counterparts, the two transfer decisions and an `organizations` descriptor; stake proven before the org row lock; DRAFT→POSTED relabel test; join/leave idempotent on retry.
