# LL-103 — Gate 7 housekeeping: authorization fence, audit completeness, isolation attempts, idempotent join/leave

**Branch** `feat/ll-103-gate7-housekeeping` · **Status** implemented · **Origin** `docs/GATE-7.md` §6 (M6, L1–L5, L10, L11 — PR #106).

## §11 — End-of-ticket report

**Ticket:** LL-103 — the Gate 7 items that needed no owner decision and no schema change.
**Files changed:** `src/server/bank-import/intercompany.ts` (`transferCounterparts` authorizes itself — M6),
`errors.ts` (`PeriodClosedInCompanyError`, a `LedgerError` carrying the company id — L1), `shared.ts`;
`eslint.config.mjs` (the LL-014 fence covers `*/shared` and `*/intercompany` — M6); shared page + action
(`closedIn=<id>` resolved to a name only when it is one of the two companies of the statement — L1);
`src/server/reports/intercompany.ts` (only CURRENT members of the organization are listed — L2);
`src/server/organizations/index.ts` (stake proven before any lock — L5; a join is audited in every existing
member — L3; join/leave idempotent on retry — L11); `src/server/accounts/internal.ts` (pair deactivation names
the leaver — L3); `tests/integration/isolation.test.ts` (attempts for the report, the counterparts, the two
transfer decisions, and an `organizations` descriptor — L4); `organizations.test.ts` (DRAFT→POSTED relabel —
L10; idempotent leave); docs.
**Migrations created:** none. **DB constraints added:** none.
**Tests added:** as listed; every isolation attempt proves the uniform denial.
**Test results:** lint ✓ / typecheck ✓ / unit ✓ / build ✓ (`npm run ci`); organizations + bank-import e2e ✓
locally; integration on CI.
**Accounting implications:** none.
**Security implications:** the one unauthorized module-root read is closed; two more internal modules are fenced
from routes; no free text is reflected from the URL into a notice; a former member's live name is no longer read.
**Decisions made not specified in the ticket:** leaving when not a member is a silent no-op (a retry after a lost
response), joining the same organization again returns it; the report drops former members entirely rather than
labelling them (they left at zero).
**Known limitations:** none.
