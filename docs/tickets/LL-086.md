# LL-086 — Invite team members

**Branch** `feat/ll-086-invite-members` · **Status** implemented · **Origin** product owner picked it
from the post-LL-085 shortlist ("start LL-086"). Design in ADR-041.

## §11 — End-of-ticket report

**Ticket:** LL-086 — Team screen: add by email (immediate or claimed-on-entry invitation), change
roles, remove members, revoke invitations; role ceiling; last-owner protection.
**Files changed:** `src/db/schema/identity.ts` (`company_invitations`, `invitation_status`),
`audit.ts` (five actions); `src/server/rbac/{capabilities,index}.ts` (`roleCovers`);
`src/validation/member.ts`; `src/server/members/{index,claim,errors}.ts` (new);
`src/server/users/index.ts` (claim hook); `src/server/companies/{index,internal}.ts`
(`addMembershipAs` removed, `lockActiveCompany` moved, `errorChainText` exported, `PURGE_ORDER`);
`src/app/members/{page,members-view,actions}.tsx` (new), `src/app/account/page.tsx` (Team link,
`you-left` notice); ADR-041; `docs/SECURITY.md`; `docs/GATE-1.md`; this ticket.
**Migrations created:** `0036_members_invitations.sql` — enum `invitation_status`; five
`audit_action` values; table `company_invitations` with `unique(company_id,id)`, three CHECKs
(lower-cased email, accepted ⇔ accepted_user_id, resolved ⇔ resolved_at), three restrict FKs, a
partial unique `(company_id, email) WHERE status='PENDING'` and a partial index `(email) WHERE
status='PENDING'`. Expand-only.
**DB constraints added:** the three CHECKs and the partial unique above.
**Tests added:** `tests/integration/members.test.ts` — add existing user (audit actor = manager),
`ALREADY_MEMBER`; PENDING invitation, `ALREADY_INVITED`, revoke + re-invite, revoked id → uniform
miss; claim on first entry with a mixed-case sign-up (ACTIVE membership, ACCEPTED + accepted_user_id,
inviter as audit actor, later entries claim nothing); three concurrent first entries → one membership,
one audit row; archived company's invitation stays PENDING; reactivation keeps the membership id;
the ceiling (ADMIN cannot grant/promote to/change/remove OWNER, each denial identical to no
membership; ADMIN is a full manager within the ceiling; OWNER can); BOOKKEEPER sees the roster only;
last-owner demote/remove → `LAST_OWNER`, allowed with a second OWNER, self-removal; never fires for
non-owners; no-op change writes no audit; the CHECKs and partial unique reject raw inserts.
`tests/integration/isolation.test.ts` — real change/remove attempts on `company_memberships`; new
`company_invitations` descriptor. `tests/integration/__adversarial.test.ts` ported to `inviteMember`.
`tests/unit/rbac.test.ts` — the ceiling. `tests/e2e/members.spec.ts` — invite → sign-up in a second
browser context claims it → role change → remove; the only owner cannot leave; OWNER offered.
**Test results:** lint ✓ / typecheck ✓ / unit ✓ / build ✓ (`npm run ci`); members e2e ✓ locally;
integration on CI.
**Accounting implications:** none.
**Security implications:** closes GATE-1 §2 (ADMIN → OWNER escalation); every membership mutation
audited; uniform denials; company always from the session context; the claim path grants only what
a manager already authorized.
**Decisions made not specified in the ticket:** ceiling as capability dominance (`roleCovers`)
rather than a new capability; last-owner rule; claim on every entry; inviter as audit actor for
claims; emails lower-cased at the boundary and CHECK-enforced; managers learn add-vs-invite.
**Known limitations:** no email is sent; no expiry/resend; no ownership-transfer wizard.
**Recommended follow-up:** email delivery once a provider exists; expiry.
