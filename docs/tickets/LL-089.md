# LL-089 — Ignore controls on the import review screen

**Branch** `feat/ll-089-ignore-controls` · **Status** implemented · **Origin** product owner: a card
statement with charges for other companies — "Can I add an ignore button next to an individual
transaction?"

## §11 — End-of-ticket report

**Ticket:** LL-089 — one-click Ignore/Undo per line, "Ignore all remaining", "Reset to suggestions",
and a live "N to post · M to ignore" count on the review screen.
**Files changed:** `src/app/bank-import/[batchId]/{review-state,line-action,bulk-controls}.tsx` (new
client components), `[batchId]/page.tsx` (provider around the table; the Action select is now the
client control seeded with the server's suggestion); this ticket.
**Migrations created:** none. **DB constraints added:** none. **Service changes:** none — decisions
are still recorded only by "Post confirmed lines" through `postImportLines`.
**Tests added:** `tests/e2e/bank-import.spec.ts` — ignore all → counts and every select ignored;
reset → back to suggestions; ignore all then undo one → post only that line, statuses IGNORED /
POSTED / IGNORED. Existing e2e cases keep driving the select directly (it is the same control).
**Test results:** lint ✓ / typecheck ✓ / unit ✓ / build ✓ (`npm run ci`); bank-import e2e ✓ locally.
**Accounting implications:** none.
**Security implications:** none — client convenience over the same form; the server re-validates.
**Decisions made not specified in the ticket:** client-side, non-persisted decisions (the existing
"nothing posts without an explicit submit" model) rather than immediate per-line server ignores;
guidance text on the review screen for foreign charges ("Due from …" vs ignore).
**Known limitations:** an un-submitted set of ignores is lost on navigation; ignored lines cannot be
un-ignored after posting.
**Recommended follow-up:** restore an IGNORED line to STAGED (a small service + button).
