# LL-080 — Chart-aware categorisation that learns from corrections

**Branch** `feat/ll-080-chart-aware-categorisation` · **Status** implemented · **Origin** product owner:
"review the chart of accounts for the specific company … and make sure it learns from the corrections
made on previous imports". Recorded as an amendment to ADR-034.

## §11 — End-of-ticket report

**Ticket:** LL-080. **Files changed:** `src/server/bank-import/extract.ts` (ExtractionContext, chart +
examples in the prompt, output constrained to the chart), `src/server/bank-import/index.ts` (context
built per import; history-first suggestion with exact then payee-key match; `historyExamples`),
tests, ADR-034 amendment, this ticket.
**Migrations created:** none. **DB constraints added:** none.
**Tests added:** integration — the extractor receives the chart (bank account excluded) and the company's
past decisions; a posted correction wins over the model's suggestion on the next import; payee-key
matching across changing reference/check numbers with exact match taking precedence and the model as
last resort. Unit — the prompt lists chart and examples; the model call carries them.
**Test results:** lint ✓ / typecheck ✓ / unit ✓ / build ✓; integration on CI.
**Accounting implications:** none — suggestions only; the reviewer still confirms every line.
**Security implications:** account names and past descriptions go to the model (§9 addendum in ADR-034);
no amounts, no ids. Suggestions are restricted to the company's own pickable accounts.
**Decisions made not specified in the ticket:** history precedes the model; payee-key fuzziness limited
to stripping digits/punctuation (no embeddings); 60 most recent distinct examples.
**Known limitations:** exact/payee-key matching only; no cross-company learning (by design).
**Recommended follow-up:** show "why" next to a suggestion (history vs model); per-company rules table.
