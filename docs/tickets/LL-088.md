# LL-088 — Import credit-card statements

**Branch** `feat/ll-088-credit-card-import` · **Status** implemented · **Origin** product owner asked how
to handle a credit-card statement; Bank Import accepted cash asset accounts only. ADR-034 amendment.

## §11 — End-of-ticket report

**Ticket:** LL-088 — Bank Import accepts credit-card liability accounts; charges increase the liability,
payments and refunds reduce it; lines post to accounts only.
**Files changed:** `src/server/accounts/statement-account.ts` (new shared `isStatementAccount`;
Reconciliation's `isReconcilableAccount` now aliases it); `src/server/bank-import/{index,extract,errors}.ts`
(card accounts accepted, `statementKind` in the extractor context and prompt, `CARD_CANNOT_APPLY`);
`src/app/bank-import/page.tsx` (card accounts offered), `[batchId]/page.tsx` (card banner, no
apply-to-document controls); ADR-034 amendment; this ticket.
**Migrations created:** none. **DB constraints added:** none.
**Tests added:** `tests/integration/bank-import.test.ts` — staging into a card passes
`statementKind='credit_card'` to the extractor; a purchase posts Dr expense / Cr card and a payment Dr
card / Cr bank (card balance −1379.50 credit-normal, bank −1500, supplies 120.50); apply_bill on a card →
`CARD_CANNOT_APPLY` with nothing posted; a non-card liability (Sales Tax Payable) → `INVALID_BANK_ACCOUNT`.
`tests/unit/bank-import-extract.test.ts` — the card paragraph in the prompt. `tests/e2e/bank-import.spec.ts`
— upload into 2100 · Credit Card, no apply controls, post all, trial balance shows 620.50 owed and balanced.
**Test results:** lint ✓ / typecheck ✓ / unit ✓ / build ✓ (`npm run ci`); bank-import e2e ✓ locally;
integration on CI.
**Accounting implications:** none to the engine — the same two-line entries; sign semantics documented.
**Security implications:** none.
**Decisions made not specified in the ticket:** one shared predicate for import and reconciliation;
card lines cannot be applied to documents (paying bills by card is a follow-up); the model is told the
statement kind rather than post-processing signs.
**Known limitations:** paying a bill by card is not modelled; the model must get the sign right (the
review screen shows every amount before posting).
**Recommended follow-up:** bill payments from a credit-card account.
