# LL-087 — Delete an uploaded bank statement

**Branch** `feat/ll-087-delete-import-batch` · **Status** implemented · **Origin** product owner:
"Add delete a download" — confirmed as removing an uploaded statement (an import batch) when nothing
from it was posted. Design in ADR-042.

## §11 — End-of-ticket report

**Ticket:** LL-087 — delete an import batch while none of its lines have posted.
**Files changed:** `src/server/bank-import/{index,errors}.ts` (`deleteImportBatch`,
`BATCH_HAS_POSTINGS`); `src/app/bank-import/actions.ts` (`deleteImportBatchAction`);
`src/app/bank-import/[batchId]/page.tsx` ("Delete this import…" disclosure while `posted === 0`,
notice); `src/app/bank-import/page.tsx` (`?ok=deleted` notice, `BATCH_NOT_FOUND`); ADR-042; this ticket.
**Migrations created:** none. **DB constraints added:** none.
**Tests added:** `tests/integration/bank-import.test.ts` — delete removes batch and lines, list and
review read as gone, ledger untouched, re-upload carries no duplicate flags; ignored lines do not
protect a batch but one POSTED line does and the refused delete rolls back; foreign/unknown batch →
`BATCH_NOT_FOUND`; BOOKKEEPER denied. `tests/e2e/bank-import.spec.ts` — delete from the review page,
list empty, old URL reads not found; after posting one line the control is absent.
**Test results:** lint ✓ / typecheck ✓ / unit ✓ / build ✓ (`npm run ci`); bank-import e2e ✓ locally;
integration on CI.
**Accounting implications:** none — a batch with any posted line cannot be deleted; posted entries
are untouched.
**Security implications:** `journal.post` re-proven; the company comes from the session; a foreign
batch id reads as not found; the log carries ids and counts only.
**Decisions made not specified in the ticket:** physical delete (not a status) for unposted batches,
recorded as ADR-042 alongside ADR-006; ignored lines do not protect a batch; log-only, no audit row.
**Known limitations:** no per-line un-staging; no "archive with content".
**Recommended follow-up:** credit-card statement import (Bank Import accepts cash asset accounts only).
