# LL-095 — Gate 6 housekeeping

**Branch** `feat/ll-095-gate6-housekeeping` · **Status** implemented · **Origin** Gate 6 §6 M6, L4–L7, N1–N9.

## §11 — End-of-ticket report

**Ticket:** LL-095 — the remaining Gate 6 items in one pass.
**Files changed:** `src/db/schema/{bank-import,reconciliation,identity}.ts`; `src/server/bank-import/index.ts`
(`mirrorOfLineId` on match + unique mapping; staged-twin duplicate flag `duplicateOf`), `normalize.ts`
(impossible US month not swapped), `extract.ts` (provider message logged only on config statuses);
`src/server/reconciliation/index.ts` (sequence rule under the account row lock; completion takes the same
lock); `src/server/ledger/index.ts` (`isIdempotencyViolation` matches only the two once-only indexes via the
cause chain); `src/lib/money-format.ts` (no `-0.00`); `src/app/reconciliation/[id]/page.tsx` (Decimal zero
check); `src/app/bank-import/[batchId]/page.tsx` (staged-twin text); `.github/workflows/ci.yml` (every
ephemeral Neon branch forks an explicit `--parent` = the project's default branch, refused if it looks like
production); ADR-034/036/038 amendments; `docs/GATE-6.md`; this ticket.
**Migrations created:** `0039_gate6_housekeeping.sql` — drops and re-adds the two statement-line FKs as
`ON DELETE RESTRICT`; adds `bank_import_lines.mirror_of_line_id` (nullable) with a same-company FK and a
UNIQUE; adds CHECKs `bank_import_lines_posted_has_entry`, `bank_import_lines_targets_only_when_posted`,
`bank_import_lines_amount_nonzero`, `companies_template_is_active`; replaces `journal_entries_immutable` so
a DRAFT delete proceeds. Expand-only; pre-checked on the dev branch (0 violating rows across 386 import lines).
**DB constraints added:** the two RESTRICT FKs, the mirror FK + unique, four CHECKs.
**Tests added:** `tests/integration/gate6-housekeeping.test.ts` — restrict FKs (batch and reconciliation
header), the three import-line CHECKs, the mirror unique, the template CHECK, DRAFT delete now succeeds /
POSTED still refused, `PURGE_ORDER` is a topological order of the FK graph from `pg_constraint`;
concurrency: delete-vs-post (three rounds, consistent either way), double submit posts once, save-vs-complete
never completes over an unsaved set, start waits for the account-row holder; `isIdempotencyViolation` cases.
Unit: `-0.00` never rendered; `13/06/2026` not swapped.
**Test results:** lint ✓ / typecheck ✓ / unit ✓ / build ✓ (`npm run ci`); bank-import + reconciliation e2e ✓
locally; integration and the migration on CI.
**Accounting implications:** none to figures; several conventional rules became structural.
**Security implications:** provider error bodies are no longer logged on non-config statuses; CI can never
fork a production-named Neon branch.
**Decisions made not specified in the ticket:** US month/day kept as the documented locale (only impossible
months are rejected); the CI parent pin resolves the default branch at run time and guards by name rather
than hard-coding an id.
**Known limitations:** the transfer-candidate lookup still offers one candidate per line.
**Recommended follow-up:** none.
