# LL-100 — Intercompany corrections: un-mark a transfer; give-back reactivates the pair; leave counts inactive pairs

**Status** proposed (Gate 7) · **Origin** `docs/GATE-7.md` §6

Gate 7 H1, 5c M1, 5b L1. `unmarkIntercompanyTransfer` (reverse this side if the group has no other side, else both sides in one transaction with the give-back lock order, `journal.post` in both; review-page Undo on POSTED INTERCOMPANY lines); `unassignSharedLine` calls `ensureIntercompanyPair` (reactivates) and uses ONE reversal date open in both companies; `removeCompanyFromOrganization` counts INACTIVE pair accounts in the balance check and the lock set. Tests: mark → unmark; take → repay → leave → rejoin → give back → leave refused.
