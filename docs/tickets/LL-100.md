# LL-100 — Intercompany corrections: un-mark a transfer; give-back reactivates the pair; leave counts inactive pairs

**Branch** `feat/ll-100-intercompany-corrections` · **Status** implemented · **Origin** `docs/GATE-7.md` §6 (H1, M1, L6 — PR #106).

## §11 — End-of-ticket report

**Ticket:** LL-100 — the three Gate 7 items that needed no owner decision.
**Files changed:** `src/server/bank-import/intercompany.ts` (`unmarkIntercompanyTransfer`), `index.ts` (export; `postedSource`
on the review view), `shared.ts` (give-back reactivates the pair via `ensureIntercompanyPair`; ONE reversal date — the
cardholder's today — open in both companies); `src/server/organizations/index.ts` (the leave rule's balance check and lock set
include INACTIVE pair accounts; only ACTIVE counterparts are row-locked — an archived one cannot post); review page + action
(`Undo transfer` on POSTED lines whose entry is INTERCOMPANY); docs.
**Migrations created:** none. **DB constraints added:** none.
**Tests added:** `tests/integration/intercompany-corrections.test.ts` — one-sided un-mark (reversed, STAGED, pair zero,
candidate gone, second call not-found, a plain posted line not un-markable); matched transfer un-marked from the other side
(both REVERSED on one date, both lines STAGED, rights needed in both companies, re-mark works); take → repay → leave →
rejoin → give back → leave refused (pair reactivated, one reversal date); a raw balance on an INACTIVE pair blocks leaving
until un-marked. E2E: undo from the payee's review page.
**Test results:** lint ✓ / typecheck ✓ / unit ✓ / build ✓ (`npm run ci`); bank-import e2e ✓ locally; integration on CI.
**Accounting implications:** corrections by reversal only; both sides of a matched transfer reverse together on one date.
**Security implications:** un-marking a matched transfer requires `journal.post` in both companies; a one-sided un-mark
only in the marking company.
**Decisions made not specified in the ticket:** the other company's statement line returns to STAGED as well (it can be
re-decided); an archived counterpart is not row-locked (it cannot post) but its balances still count.
**Known limitations:** `docs/GATE-7.md` (PR #106) marks H1/M1/L6 fixed once that PR and this one are both on `main`.
