# LL-093 — Gate 6 fix-now items (M4, L1, L2)

**Branch** `feat/ll-093-gate6-fix-now` · **Status** implemented · **Origin** Gate 6 §6 "fix now".

## §11 — End-of-ticket report

**Ticket:** LL-093 — three small Gate 6 fixes.
**Files changed:** `src/app/reconciliation/update-fields.ts` (new pure helper), `actions.ts`, `[id]/page.tsx`
(the "Update statement" form sends only fields the reviewer changed — an untouched two-decimal display never
rewrites the stored four-decimal figure; a no-change submit is a no-op); `src/app/bank-import/actions.ts`
(`postImportLinesAction` validates `batchId` as a uuid → not-found, never a database error);
`src/server/bank-import/index.ts` (the card guard is computed once before the decisions loop and checked per
LIVE decision, so a stale re-submit carrying an apply decision for an already-decided line is an idempotent
no-op instead of a refusal); `docs/GATE-6.md` (M4/L1/L2 marked fixed); this ticket.
**Migrations created:** none. **DB constraints added:** none.
**Tests added:** `tests/unit/reconciliation-update-fields.test.ts` (unchanged → nothing sent; date only;
typed amount at its precision; missing fields); `tests/integration/bank-import.test.ts` — a card batch
re-submit with an apply decision on an IGNORED line posts the live lines, while a live apply on a card
line is still `CARD_CANNOT_APPLY`.
**Test results:** lint ✓ / typecheck ✓ / unit ✓ / build ✓ (`npm run ci`); reconciliation + bank-import
e2e ✓ locally; integration on CI.
**Accounting implications:** a stored statement figure is no longer changed by a display rounding.
**Security implications:** one malformed-id 500 removed.
**Decisions made not specified in the ticket:** the form carries the shown values as hidden fields and the
action compares text, so a reviewer who retypes the same two decimals also sends nothing.
**Known limitations:** none. **Recommended follow-up:** none.
