# LL-063 — Vendor credits (debit memos)

**Branch** `feat/ll-063-vendor-credits`

**Status** Backlog · **Origin** Sprint 6 plan (Accounts Payable). See
[SPRINT-6-PLAN.md](../SPRINT-6-PLAN.md). The structural mirror of LL-051 (customer credit memos).

> A vendor credit (debit memo) reduces what we owe a vendor without cash — a return or allowance
> against a bill. The A/P analogue of a credit memo.
>
> ### Schema
> A `vendor_credits` table mirroring `credit_memos`: `vendor_id`, `bill_id`, `credit_date`, `amount`,
> a contra `account_id` (the expense/returns account credited), `status` (`POSTED`/`VOID`),
> composite FKs, positivity CHECK. One migration.
>
> ### Posting (through `LedgerService`)
> **Dr A/P (vendor-tagged) / Cr Expense-contra**, source type reusing the A/P convention, reducing
> the bill's open balance exactly like a bill payment does. A credit that clears the bill marks it
> **PAID**; voiding reopens it. Applied per-bill (mirror ADR-019).
>
> ### Shared A/P open-balance
> Extend the **shared A/P open-balance derivation** (the mirror of `open-balance.ts`): a bill's open
> balance = `total − Σ(non-void bill-payments + vendor-credits)`, used by the aging (LL-064) and the
> over-application guards. Extend **`voidBill`'s adjustments guard** (mirror `INVOICE_HAS_ADJUSTMENTS`)
> to count vendor credits, so voiding a bill with a live credit is refused.
>
> ### Rules
> `vendor_credit.view` (EVERYONE) / `vendor_credit.create` (ALL_WRITERS) / `vendor_credit.void`
> (LEDGER_WRITERS); update the rbac matrix. Over-credit beyond open balance rejected (decimal.js).
>
> ### Tests
> Credit reduces the payable and reconciles (subsidiary⇔control moves together, **GL-Txxx**); clears
> → PAID; void restores; over-credit rejected; voiding a bill with a live credit refused. Company
> scoping / no leak.
>
> ### Not in scope
> Unapplied vendor credit (credit not tied to a bill); refunds from a vendor; the UI (LL-065).

---

## §11 — End-of-ticket report

**Ticket** LL-063 — Vendor credits (debit memos) · **Branch** `feat/ll-063-vendor-credits`

**Files changed**
- `src/db/schema/vendor-credits.ts` (new) — `vendor_credit_status` enum + `vendor_credits` table
  (bill / vendor / expense-account composite FKs, `amount>0` CHECK); `ledger.ts` — `VENDOR_CREDIT`
  source type; `audit.ts` — `VENDOR_CREDIT_ISSUED/VOIDED`; `schema/index.ts` — export.
- `src/server/reports/bill-open-balance.ts` — `billReductionsExpr` / `billReductionsTotal` now also
  sum non-void `vendor_credits` (the shared A/P open-balance derivation — the ONE place).
- `src/server/vendor-credits/{index,errors}.ts` (new) — `issueVendorCredit` (Dr A/P vendor-tagged /
  Cr expense) / `voidVendorCredit` / `getVendorCredit` / `listVendorCredits`, mirroring the
  credit-memo service.
- `src/server/bills/{index,errors}.ts` — `voidBill` now also refuses a bill with live vendor credits
  (`BILL_HAS_ADJUSTMENTS`); with `BILL_HAS_PAYMENTS` the void guard is complete.
- `src/validation/vendor-credit.ts` (new); `src/server/rbac/capabilities.ts` — `vendor_credit.view`
  (EVERYONE) / `vendor_credit.create` (ALL_WRITERS) / `vendor_credit.void` (LEDGER_WRITERS) + matrix.

**Migrations created**
- `drizzle/migrations/0024_handy_thor.sql` — `CREATE TYPE vendor_credit_status ('POSTED','VOID')`;
  three `ALTER TYPE … ADD VALUE` (audit `VENDOR_CREDIT_ISSUED/VOIDED`, source `VENDOR_CREDIT`);
  `CREATE TABLE vendor_credits` with the `(company_id,id)` unique, the `amount>0` CHECK, the three
  composite FKs (bill / vendor / expense-account, all `ON DELETE restrict`), and three indexes. **No
  trigger or function changes** — the 0023 control-account guard already allows a `VENDOR_CREDIT`
  source to post to A/P. No column drops, no data transforms. Applied to the dev branch only.

**DB constraints added** `vendor_credits_company_id_id_unique`; `vendor_credits_amount_positive`
(CHECK > 0); composite FKs `vendor_credits_{bill,vendor,expense_account}_same_company_fk`.

**Tests added**
- `vendor-credits.test.ts`: full credit → PAID + A/P to zero (Dr A/P vendor-tagged / Cr expense);
  partial → OPEN with a reduced open balance + `listOpenBills`; a payment + a credit clear one bill;
  `CREDIT_EXCEEDS_BALANCE`; `CREDIT_ACCOUNT_INVALID` (non-expense / inactive); `BILL_NOT_OPEN`
  (draft); `PERIOD_CLOSED`; cross-company `BILL_NOT_FOUND`; void reverses + reopens; double-void →
  `VENDOR_CREDIT_NOT_POSTED`; the **voidBill `BILL_HAS_ADJUSTMENTS`** guard (void the credit first);
  authorization (BOOKKEEPER issues, cannot void; ACCOUNTANT voids).
- `gl-regression.test.ts` **GL-T025**: a vendor credit reduces the A/P control; void restores it.
- `isolation.test.ts`: a `vendor_credits` descriptor (list/read/void all denied) + the structural
  completeness check; `rbac.test.ts`: the three new capabilities across the exhaustive matrix.

**Test results** lint ✅ · typecheck ✅ · unit ✅ 177 (incl. rbac matrix) · build ✅; integration
(affected suites): vendor-credits, bills, bill-payments, control-account-guard ✅ 43 · gl-regression
(incl. GL-T025) ✅ 30 · isolation (incl. completeness) ✅ 5. The full integration suite is the CI
release gate.

**Accounting implications** A vendor credit posts Dr A/P (vendor-tagged) / Cr an expense account
through `LedgerService`, reducing one bill's open balance in the A/P subsidiary exactly like a bill
payment; a fully-credited bill reads PAID; void reverses and reopens. The A/P reduction-source loop
is now closed like A/R: shared derivation + `voidBill` guard + GL regression + ADR-023 all move
together, so the A/P aging⇔control tie (LL-064) stays structural.

**Security implications** `vendor_credit.*`-gated, company-scoped, no existence leak (a cross-company
id reads as a genuine miss). The expense account is validated in-company / ACTIVE / EXPENSE. A
cross-tenant bill / vendor / expense-account reference is structurally impossible (composite FKs).

**Decisions made that were not specified in the ticket** New `VENDOR_CREDIT` source type (named in
the plan). The isolation `vendor_credits` seed resolves-or-creates the A/P control (every descriptor
attacks one shared victim and `(company_id, system_account_type)` is unique, so it must reuse the A/P
that the `bill_payments` descriptor tags) — a test-harness fix, not a code change.

**Known limitations** Vendor credits are applied per-bill; unapplied vendor credit and vendor refunds
are deferred (ADR-023), as they need the A/P subsidiary to carry credit balances. No A/P aging /
vendor statement (LL-064) or UI (LL-065) yet. Crediting a non-expense account (e.g. an inventory
return to an asset) is a future enhancement.

**Recommended follow-up** LL-064 (A/P aging + vendor statement — reads the extended open balance),
then LL-065 (A/P reporting UI), then GATE 5 (closes Sprint 6).
