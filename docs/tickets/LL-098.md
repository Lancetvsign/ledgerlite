# LL-098 — Intercompany balances report + GL-T029 (Sprint 7, ticket 3)

**Branch** `feat/ll-098-intercompany-report` · **Status** implemented · **Origin** `docs/SPRINT-7-PLAN.md` §5;
ADR-043 ("GL-T029 proves Σ Due-from-B in A = Σ Due-to-A in B for every pair").

## §11 — End-of-ticket report

**Ticket:** LL-098 — a per-company intercompany balances report with the counterpart's mirror figure and
the difference, and the mirror invariant in the release gate.
**Files changed:** `src/server/reports/intercompany.ts` (new) + barrel; `src/server/ledger/invariants.ts`
(`findIntercompanyMismatches`, `assertIntercompanyMirror`) + barrel; `src/app/reports/intercompany/page.tsx`
(new); `src/app/reports/page.tsx` (link); docs.
**Migrations created:** none. **DB constraints added:** none.
**Tests added:** `tests/integration/intercompany-report.test.ts` — both companies show the same figure with a
zero difference before and after a give-back, as-of dating, no-organization and denial cases; a one-sided raw
INTERCOMPANY posting inside a rolled-back transaction is reported by the invariant and flips the report to
MISMATCH. `tests/integration/gl-regression.test.ts` — **GL-T029**: three companies, a shared card statement
split two ways plus a give-back; every report mirrors, totals agree per company, `assertIntercompanyMirror`
passes. E2E: the report is listed and renders the empty state outside an organization.
**Test results:** lint ✓ / typecheck ✓ / unit ✓ / build ✓ (`npm run ci`); reports e2e ✓ locally; integration
+ GL suite on CI.
**Accounting implications:** none to figures; the mirror rule (ADR-043) is now stated as an invariant and
proven at the gate.
**Security implications:** the counterpart's mirror balance is a cross-company read by design, limited to the
pair account facing this company; `report.view` in this company authorizes it. Nothing else of the
counterpart's books is exposed.
**Decisions made not specified in the ticket:** the mirror check is explicit (gate + report), not part of the
per-test `assertLedgerIntegrity` teardown, because fixtures seed one-sided rows on purpose; differences are
computed with Decimal from the database's NUMERIC strings (the only arithmetic outside SQL here).
**Known limitations:** the report lists pairs only (no drill-down to the entries); the register covers that.
**Recommended follow-up:** LL-099 — intercompany bank transfers and settlement, after which these balances
net to zero through the transfer flow.
