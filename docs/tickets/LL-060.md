# LL-060 — Vendors

**Branch** `feat/ll-060-vendors`

**Status** Backlog · **Origin** Sprint 6 plan (Accounts Payable). See
[SPRINT-6-PLAN.md](../SPRINT-6-PLAN.md). The structural mirror of LL-040 (customers) — the party a
**bill** owes.

> The vendor entity that bills and bill payments reference. A direct mirror of customers (LL-040):
> **no ledger effect**, no stored balance (invariant 2) — a vendor's A/P balance is derived from
> journal lines, later.
>
> ### Schema
> A `vendors` table: `id`, `company_id`, `name` (required), optional `vendor_number`, contact
> fields, `status` (`ACTIVE`/`INACTIVE` — deactivate, never delete, ADR-006), timestamps.
> Composite-FK tenancy: `UNIQUE (company_id, id)` — the hook the ledger's
> `journal_lines (company_id, vendor_id)` FK builds on, so a line can never reference another
> tenant's vendor. `UNIQUE (company_id, vendor_number)` and a `(company_id, name)` index.
> One migration.
>
> ### Service
> `createVendor` / `updateVendor` / `deactivateVendor` / `getVendor` / `listVendors`, company-scoped,
> mirroring the customer service. A cross-company / unknown id reads as a genuine miss (no leak, §6).
>
> ### Rules
> New capabilities **`vendor.view`** (EVERYONE) and **`vendor.manage`** (ALL_WRITERS); update the
> exhaustive `rbac.test.ts` matrix (known gotcha — run `npm run ci` before pushing). Money N/A here.
>
> ### Tests
> CRUD + deactivate; company scoping / no existence leak; the isolation descriptor for `vendors`;
> the rbac matrix.
>
> ### Not in scope
> Bills (LL-061); any A/P balance (derived, LL-064); vendor merge; 1099 fields.

---

## §11 — End-of-ticket report

**Ticket** LL-060 — Vendors

**Branch** `feat/ll-060-vendors`

**Files changed**
- `src/db/schema/vendors.ts` (new) — the `vendors` table + `vendor_status` enum, mirroring
  customers (`address` in place of `billing_address`).
- `src/db/schema/ledger.ts` — added the `journal_lines_vendor_same_company_fk` composite FK
  `(company_id, vendor_id) → vendors (company_id, id)`.
- `src/db/schema/audit.ts` — added `VENDOR_CREATED` / `VENDOR_UPDATED` / `VENDOR_DEACTIVATED` to the
  `audit_action` enum; `src/db/schema/index.ts` — export vendors.
- `src/server/vendors/{index,errors}.ts` (new) — createVendor / updateVendor / deactivateVendor /
  listVendors, company-scoped, audited, soft-delete (ADR-006), mirroring the customer service.
- `src/validation/vendor.ts` (new) — create/update input (mirror of customer).
- `src/server/rbac/capabilities.ts` — `vendor.view` (EVERYONE), `vendor.manage` (ALL_WRITERS).
- `tests/integration/vendors.test.ts` (new), `tests/integration/isolation.test.ts` (vendors
  descriptor), `tests/unit/rbac.test.ts` (matrix).

**Migrations created**
- `drizzle/migrations/0021_spooky_the_order.sql` — creates the `vendor_status` enum; adds three
  `VENDOR_*` values to `audit_action`; creates `vendors` with `UNIQUE (company_id, id)` +
  `UNIQUE (company_id, vendor_number)`, the companies FK (`ON DELETE restrict`), and the
  `(company_id, name)` index; adds the `journal_lines → vendors` composite FK. No column drops, no
  data transforms. Applied to the dev branch only.

**DB constraints added** `vendors_company_id_id_unique`, `vendors_company_number_unique`,
`vendors_company_id_companies_id_fk`, `journal_lines_vendor_same_company_fk`.

**Tests added**
- `vendors.test.ts` (11): CRUD + audit; soft-delete stays listable; duplicate vendor number
  rejected; NULL numbers distinct; company-scoped listing/ordering; cross-company update →
  `VENDOR_NOT_FOUND`; authorization (READ_ONLY view-not-manage, non-member denied); **structural
  FK** — a raw journal line tagged with another company's vendor is rejected, an own-company one
  accepted.
- `isolation.test.ts` — a `vendors` descriptor (cross-tenant list/create/update/deactivate denied);
  the completeness test now covers vendors.
- `rbac.test.ts` — the exhaustive matrix gains `vendor.view` (all roles) / `vendor.manage` (writers).

**Test results** lint ✅ · typecheck ✅ · unit ✅ (incl. rbac matrix) · build ✅ (`npm run ci`);
integration: vendors + isolation ✅ (16/16); full suite ✅ **427/427**.

**Accounting implications** None — no ledger effect, no stored balance (invariant 2). Installs the
tenancy hook (`journal_lines → vendors` FK) so the vendor-tagged A/P lines LL-061+ will post are
structurally confined to their company, exactly like the customer tag.

**Security implications** `vendor.manage`/`vendor.view`-gated and company-scoped; a cross-company /
unknown id reads as a genuine miss (no existence leak, §6). The composite FK makes a cross-tenant
vendor reference impossible in the database.

**Decisions made that were not specified in the ticket** Named the address field `address` (not
customers' `billing_address`) — a vendor's is a remit-to address. Mirrored the customer service
surface exactly, so there is no single `getVendor` yet (LL-061 will add an internal loader as
invoices did). The `journal_lines` vendor FK, only anticipated by the column before, is added now.

**Known limitations** No bills/posting/balance yet (LL-061/064). Vendor/customer services are
near-twins — a shared-core refactor is on the Sprint 7+ backlog.

**Recommended follow-up** LL-061 (bills) posts Dr Expense / Cr A/P vendor-tagged, relying on this FK.

