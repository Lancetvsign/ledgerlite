# LL-081 — Credit-card reconciliation

**Branch** `feat/ll-081-credit-card-reconciliation` · **Status** implemented · **Origin** product owner:
"next build credit card reconciliation". Amendment to ADR-036.

## §11 — End-of-ticket report

**Ticket:** LL-081. **Files changed:** `src/server/reconciliation/index.ts` (`isReconcilableAccount`;
account-aware sign in the figures, tick-list amounts and the completion check), `src/app/reconciliation/
page.tsx` (picker + copy), ADR-036 amendment, tests, this ticket.
**Migrations created:** none — the standard chart already marks `2100 Credit Card` with
`accountSubtype = 'credit_card'`. **DB constraints added:** none.
**Tests added:** integration — a credit-card account reconciles: a charge (Cr card) shows positive, a payment
(Dr card) negative, the statement's balance owed completes at zero; a plain liability (no subtype) is
refused; ledger-as-of uses the card's sign.
**Test results:** lint ✓ / typecheck ✓ / unit ✓ / build ✓; integration on CI.
**Accounting implications:** none to the ledger; the control now covers liability statements with the
correct sign.
**Security implications:** none.
**Decisions made not specified in the ticket:** the marker is the existing `credit_card` subtype (no new
column); statement figure entered as the card states it (balance owed, positive).
**Known limitations:** bank-statement IMPORT still targets cash accounts only — importing a card statement
(charges as expenses, payments as transfers) is a follow-up.
**Recommended follow-up:** credit-card statement import; auto-matching.
