# LL-079 — Money displayed at two decimals

**Branch** `feat/money-display-2dp` · **Status** implemented · **Origin** product owner: "all accounting
numbers shown with only 2 places after decimal". Design in ADR-037.

## §11 — End-of-ticket report

**Ticket:** LL-079 — 2-dp money display with thousands separators.
**Files changed:** `src/lib/money-format.ts` (new: `formatMoney`, `toInputAmount`); every money cell in
`src/app/**` (dashboard, reports ×8, invoices, bills, payments, bill payments, bank-import review,
reconciliation, opening balances; the two client payment forms' advisory totals); e2e assertions
(31) moved to the display form; ADR-037; this ticket.
**Migrations created:** none. **DB constraints added:** none.
**Tests added:** `tests/unit/money-format.test.ts` — grouping, sign, half-even rounding at cents,
Decimal input, input form. e2e suites assert `1,379.50`-style text.
**Test results:** lint ✓ / typecheck ✓ / unit ✓ / build ✓ (`npm run ci`); e2e on CI.
**Accounting implications:** none — storage, computation and comparisons are unchanged at NUMERIC(19,4);
display rounding is half-even and presentation-only (ADR-037 notes the visible-sum caveat).
**Security implications:** none.
**Decisions made not specified in the ticket:** thousands separators added (accounting convention);
form inputs keep the plain two-decimal form; raw strings retained for all logic.
**Known limitations:** no locale/currency symbol; negative shown with a leading minus, not parentheses.
**Recommended follow-up:** company display preferences (locale, currency symbol) in the same helper.
