# LL-007 — Engineering Documentation


**Branch** `docs/ll-007-engineering-documentation`

> Implement LL-007. Populate `/docs`: `ARCHITECTURE.md`, `ACCOUNTING_RULES.md`,
> `DATABASE.md`, `API.md`, `SECURITY.md`, `TESTING.md`, `DEPLOYMENT.md`, and extend
> `DECISIONS.md`.
>
> Source of truth is the approved PRD plus the ADRs from LL-000. Where documentation
> and implementation disagree, flag the discrepancy — do not paper over it.
>
> `ACCOUNTING_RULES.md` must state prominently, each with the mechanism that enforces
> it and the layer it lives in:
> - debits equal credits
> - posted journal entries are immutable
> - no authoritative account-balance field exists anywhere
> - no floating-point money
> - company isolation
> - closed period protection
> - idempotent financial posting
> - database transaction requirement
>
> For each rule, distinguish **enforced by database constraint** from **enforced by
> application code** from **enforced by test only**. That distinction is the honest
> measure of how strong each guarantee actually is, and it will guide LL-030.
>
> Write for a future Claude or Codex session that has no chat history and must reach
> correct conclusions from the repository alone.

---

