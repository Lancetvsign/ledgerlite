# LL-102 — Shared-path hygiene: card payments stay with the cardholder; pair choice reduces the open balance

**Branch** `feat/ll-102-shared-path-hygiene` · **Status** implemented · **Origin** `docs/GATE-7.md` §6 (M3, M4, L9 — PR #106).
The PERSONAL-account restriction (5b N2) awaits the owner's §7 decision and is NOT in this ticket.

## §11 — End-of-ticket report

**Ticket:** LL-102 — the shared-path and pair-choice items of Gate 7 that needed no owner decision.
**Files changed:** `src/server/bank-import/shared.ts` (`cardPaymentLines`: positive lines with a same-company transfer
candidate hidden from `getSharedImportBatch` and refused by `assignSharedLines` — `CARD_PAYMENT_NOT_TAKEABLE`),
`index.ts` (`findTransferCandidates` exported; a card CHARGE marked as a transfer refused — `CARD_CHARGE_NOT_TRANSFER`),
`intercompany.ts` (`pairAccountsFor` chooses by open balance, sees deactivated pairs), `errors.ts`; review page
(the mark offered only on positive card lines; notices); shared page notice; GL-T029 reworked (the card payment is
matched by the cardholder, C takes a refund, the taker's P&L asserted); docs.
**Migrations created:** none. **DB constraints added:** none.
**Tests added:** `tests/integration/intercompany-hygiene.test.ts` — payments with a POSTED or STAGED bank mirror hidden and
refused, a refund takeable; card charge mark refused, refund mark allowed; both-direction pairs: a repayment pays down the
payable, then the relationship nets to zero and leaving works; after leave + rejoin the deactivated pair is reused.
GL-T029 updated.
**Test results:** lint ✓ / typecheck ✓ / unit ✓ / build ✓ (`npm run ci`); bank-import + reports e2e ✓ locally;
integration + GL suite on CI.
**Accounting implications:** a card payment can no longer be booked as another company's negative expense; repayments
settle balances instead of opening opposite ones.
**Security implications:** none new.
**Decisions made not specified in the ticket:** "payment" is detected by a same-company mirror (LL-094's candidate
finder), not by description; a positive card line without a mirror is treated as a refund and stays takeable.
**Known limitations:** a card payment whose bank statement has not been imported yet has no mirror and is therefore
still on offer until the bank statement is staged.
