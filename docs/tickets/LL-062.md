# LL-062 — Bill payments + A/P control-account lock

**Branch** `feat/ll-062-bill-payments`

**Status** Backlog · **Origin** Sprint 6 plan (Accounts Payable). See
[SPRINT-6-PLAN.md](../SPRINT-6-PLAN.md). Mirror of LL-043/045 (payments); extends ADR-018 (the 0018
control-account guard) to A/P.

> Paying a vendor settles the payable. The mirror of receiving a customer payment, with the cash
> flowing the other way.
>
> ### Schema
> A `bill_payments` (+ `bill_payment_applications`) pair mirroring payments/applications: `vendor_id`,
> `payment_date`, a `cash_account_id` (the account money leaves from), `amount` (= Σ applications),
> composite FKs to same-company vendor / bills / cash account, positivity CHECKs, and a
> `(bill_payment_id, bill_id)` unique. One migration.
>
> ### Posting (through `LedgerService`)
> **Dr A/P (`amount`, vendor-tagged) / Cr Cash**, applied to the vendor's OPEN bills (each locked
> `FOR UPDATE`, must belong to the vendor, ≤ its open balance), marking a bill **PAID** when its
> open balance reaches zero. The cash account must be in-company, ACTIVE, ASSET, and **not** the A/P
> control. `voidBillPayment` reverses and reverts PAID→OPEN under an explicit `FOR UPDATE`.
>
> ### A/P control-account lock (structural)
> **Generalize the LL-050 0018 trigger** so a manual `JOURNAL_ENTRY` line into the **A/P** control
> account is rejected too — one `BEFORE INSERT` guard covering every control account that has a
> subsidiary (A/R and A/P). Keeps A/P moved only by documents, so the aging⇔control tie is
> structural. Migration replaces the trigger function (mirror of 0018).
>
> ### Rules
> `bill_payment.view` (EVERYONE) / `bill_payment.create` (ALL_WRITERS) / `bill_payment.void`
> (LEDGER_WRITERS); update the rbac matrix. Over-application blocked with decimal.js vs the derived
> open balance.
>
> ### Tests
> Full/partial payment; PAID←→OPEN on void; over-apply / wrong-vendor / non-asset-cash rejected;
> **raw-SQL** manual JE into A/P **rejected** (structural), A/R still rejected, other system accounts
> still postable; **GL-Txxx** bill-payment reconciliation.
>
> ### Not in scope
> Vendor credits (LL-063); the aging/statement (LL-064); electronic payment / check printing.

---

## §11 — End-of-ticket report

**Ticket** LL-062 — Bill payments + A/P control-account lock · **Branch** `feat/ll-062-bill-payments`

**Files changed**
- `src/db/schema/bill-payments.ts` (new) — `bill_payments` + `bill_payment_applications` (mirror of
  payments); `ledger.ts` — `BILL_PAYMENT` source type; `audit.ts` — `BILL_PAYMENT_MADE/VOIDED`;
  `index.ts` — export.
- `src/server/reports/bill-open-balance.ts` (new) — `billReductionsExpr` / `billReductionsTotal`
  (the shared A/P open-balance derivation, mirror of `open-balance.ts`).
- `src/server/bill-payments/{index,errors}.ts` (new) — `payBill` / `voidBillPayment` /
  `getBillPayment` / `listBillPayments` / `listOpenBills` mirroring the payment service (Dr A/P
  vendor-tagged / Cr cash).
- `src/server/bills/{index,errors}.ts` — `voidBill` now refuses a bill with live payments
  (`BILL_HAS_PAYMENTS`, the Gate-4 lesson applied to A/P).
- `src/validation/bill-payment.ts` (new); `src/server/rbac/capabilities.ts` — `bill_payment.view`
  (EVERYONE) / `bill_payment.create` (ALL_WRITERS) / `bill_payment.void` (LEDGER_WRITERS) + matrix.

**Migrations created**
- `drizzle/migrations/0023_busy_spitfire.sql` — `bill_payment_status` enum; `BILL_PAYMENT` source
  type; two `BILL_PAYMENT_*` audit values; `bill_payments` + `bill_payment_applications` (composite
  FKs, positivity CHECKs); and a **hand-added trigger generalization**: DROP the A/R-only guard
  (`assert_no_manual_post_to_ar` / `journal_lines_no_manual_ar_post`), CREATE a control-account guard
  (`assert_no_manual_post_to_control_account` / `journal_lines_no_manual_control_post`) that rejects a
  manual `JOURNAL_ENTRY` line into **A/R or A/P**. No column drops, no data transforms. Applied to the
  dev branch only.

**DB constraints added** `bill_payments_*` (vendor / cash-account composite FKs, amount>0),
`bill_payment_applications_*` (payment / bill composite FKs, amount>0, (payment,bill) unique); the
generalized control-account trigger.

**Tests added**
- `bill-payments.test.ts`: full payment → PAID + A/P to zero (Dr A/P vendor-tagged / Cr cash);
  partial → OPEN with a reduced open balance; OVERAPPLIED; BILL_WRONG_VENDOR; cash account
  non-asset / A/P / **A/R** → CASH_ACCOUNT_INVALID; void reverses + reverts PAID→OPEN; double-void;
  the **voidBill BILL_HAS_PAYMENTS** guard (void the payment first); authorization.
- `control-account-guard.test.ts`: a raw-SQL manual JE into **A/P** is rejected (structural), like A/R.
- `gl-regression.test.ts` **GL-T024**: a bill payment reduces the A/P control; void restores it.
- `isolation.test.ts`: `bill_payments` + `bill_payment_applications` descriptors; rbac matrix.

**Test results** lint ✅ · typecheck ✅ · unit ✅ (incl. rbac matrix) · build ✅; integration:
bill-payments, control-account-guard, gl-regression, isolation ✅; full suite **451/451** ✅.

**Accounting implications** A bill payment posts Dr A/P (vendor-tagged) / Cr cash through
`LedgerService`; a fully-paid bill reads PAID; void reverses and reopens. **A/P is now structurally
locked** against manual journal entry (the 0018 guard generalized to A/R + A/P), so A/P moves only
through documents — the A/P aging⇔control tie (LL-064) will be structural.

**Security implications** `bill_payment.*`-gated, company-scoped, no existence leak; the cash account
is validated ASSET / ACTIVE / **not a control account** (A/R or A/P). A cross-tenant vendor / cash /
bill reference is impossible (composite FKs).

**Decisions made that were not specified in the ticket** New `BILL_PAYMENT` source type. Found &
fixed during review: `payBill` originally rejected only A/P as the cash account, but A/R is also an
ASSET — a crafted request could credit A/R and break its reconciliation; now any control account is
refused. The 0018 guard was renamed to a generic control-account guard (AGENTS §0).

**Known limitations** `voidBill` guards live payments but not vendor credits yet — **LL-063 MUST add
`BILL_HAS_ADJUSTMENTS`**. No aging/statement (LL-064) or UI (LL-065). Bill-payments/payments are
near-twins (shared-core refactor backlogged).

**Recommended follow-up** LL-063 (vendor credits — extend `bill-open-balance` + the void guard).
